{
  "name": "astrogem-cutter-web",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc -p tsconfig.check.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "~5.9.3",
    "vite": "^7.1.12",
    "vitest": "^2.1.0"
  }
}
